import { LightningElement, track } from 'lwc';

export default class EducationCmp extends LightningElement {
    // @track
    // count = [1];

    //pobieranie z bazy, potem forEach

    addEducationSection(){
        // this.count.push(4)
    }

    removeEducationSection(){
        // this.count.push(4)
    }
}